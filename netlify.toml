[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Keep build dependencies out of the published static site
[build.processing]
  skip_processing = false

# redeploy trigger

# rebuild 1781475743
